import { FuelCard } from './fuel-card';

describe('FuelCard', () => {
  it('should create an instance', () => {
    expect(new FuelCard(null, null, null, null, null, null, null, null, null)).toBeTruthy();
  });
});
